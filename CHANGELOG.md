# 更新日志 (Changelog)

所有关键版本的更新历史都将记录于此文件中。

## [1.4.1] - 2026-09-11

### 🛠️ 核心修复与多章节同名课程支持 (Fixes & Multi-Section Support)

- **彻底支持多章节同名课程（如“系统登录—SSO单点登录”）独立抓取、存储与播放**：
  - **抓取阶段保留章节信息并启用复合键去重**：增强章节提取逻辑（`sectionTitleFrom`），不仅支持“第X期/模块X”，更全面支持“01总公司”、“02分支机构”等通用折叠大章节的识别；目录去重由单维度标题判定升级为“章节名称 + 小节标题”复合判定，彻底解决同专题多章节下同名小节被当作重复项误丢弃的缺陷。
  - **唯一键与数据库存储隔离**：在 `externalIdFrom` 与 Rust 后端 `import_capture` 中将所属章节名称融入 `external_id`（如 `01总公司_01. 系统登录—SSO单点登录`），彻底杜绝底层 SQLite 数据库 `UNIQUE(topic_id, external_id)` 唯一性约束导致同名课程被覆盖合并的问题。
  - **播放定位脚本按章节范围精确限定**：点击播放脚本（`merchant_course_click_script`）引入章节名称参数；播放时先智能识别并展开目标小节所在的大章节折叠容器，限定在当前章节 DOM 范围内进行小节检索与点击触发，杜绝跨章节误点同名课程或折叠未展开导致的看门狗超时异常跳过。

## [1.4.0] - 2026-09-11

### 🛠️ 核心修复与完播防误切加固 (Fixes & Playback Integrity)

- **彻底根治视频切课瞬间误判完播缺陷（秒切课/串读缺陷）**：
  - **视频与文档探测通道严格物理隔离**：在播放控制桥接脚本中绑定课程类型（`currentCourseKind`）。视频课程（`video`）在网页播放器加载与 `<video>` 元素挂载就绪前，强制禁止回落至文档大纲扫描分支，从源头杜绝因播放器初始化耗时而误读上一门课已学完进度。
  - **大纲小节序号严格提取与双向校验（`matchesCurrentCourse`）**：在目录扫描与大纲徽章（如 `[上次学习]`）提取逻辑中增加小节序号匹配校验。即使系列课程主体标题完全一致（如多期同名课），只要前置序号不匹配（如 `04.` 与 `03.`），坚决不采纳，杜绝跨课串读。
  - **目标课程点击定位防护（`matchesTarget`）**：在课程点击跳转脚本中增加目标小节序号校验，避免因主体标题前缀重叠误点击同一专题下的其他小节。
  - **Rust 后端启动绝对时间硬防抖**：`ActiveCourse` 记录课程启动绝对时间戳 `started_at`，无论播放器状态是否已提前流转至 `playing`，课程启动后 8 秒内收到的任何 `ended` 完播事件一律视为上一课残留噪点予以丢弃，消除防抖被首帧状态变更绕过的隐患。
  - **长视频伪造完播包硬核熔断**：针对已知时长 > 120 秒的视频课程，若收到来自文档模拟或异常弹窗合成的 `100|100`（时长为 100）完播包，Rust 端坚决拦截丢弃，杜绝数千秒的长视频被覆盖为 100 秒秒判完成。
  - **文档完播 10 秒硬性驻留保护**：文档与 PPT 课件探测增加页面至少正常加载驻留 10 秒后才允许触发完播的保护机制，防止页面初始渲染未完成时读到旧状态造成误报。
  - **SessionStorage 跨页面上下文延续**：将当前课程信息持久化写入 Webview 的 `sessionStorage`，使页面重载或导航跳转后能第一时间继承正确的课程标题与类型，杜绝状态真空期。

### 🎨 交互与体验优化 (UX Improvements)

- **专题入口按钮文案与提示智能化**：
  - 根据各平台的专题学习进度与登录态，自动将操作按钮动态切换为更具引导性的「直达当前专题」、「打开最近专题」或「打开完成登录」；
  - 增加详尽的 Hover 提示（Tooltip），明确说明各入口的直达逻辑与使用场景。

---

## [1.3.9] - 2026-09-11

### 🛠️ 修复与数据采集精准度升级 (Fixes & Capture Precision)

- **商户培训平台（YS学堂）抓取精准度大幅升级（彻底根除问卷误判与跨条目文本污染）**：
  - **DOM 作用域边界隔离（`getItemScope`）**：在提取卡片文本与计算课程特征时，严格限定在单门小节条目的局部 DOM 范围内，严禁向上跨越包含所有小节的外层列表容器，彻底根除因列表内某一项包含“问卷/评价表”导致全列表所有课程（包括线下课与视频）被全量误判为 `survey` 的恶性缺陷。
  - **单课独立徽章精确匹配与类型严格互斥**：条目类型严格提取自身独立的徽章标签（Tag/Badge）。带有 `[线下课]` 或标题包含线下课特征的条目精准识别为 `offline`（线下课）；带有 `[问卷]`、`[评价表]` 的条目精准识别为 `survey`（问卷）；两者严格互斥，精准分类。
  - **优先定位“培训内容”Tab 面板**：支持 Ant Design Tabs 激活的培训内容面板识别以及单章节容器识别，优先限定在该内容区域内解析小节，从结构层彻底将详情页头部的封面、学习人数、起止时间及学分统计卡片隔离在目录解析之外。
- **详情页头部概览元数据过滤强化**：
  - 在 `isMeta` 与 `isInvalidTopicTitle` 中增强数字比值（如 `0 / 3.33`、`0 / 2`）、空格分隔的日期时间范围（如 `起止时间 2026-04-23 13:00 ~ ...`）、纯百分比以及统计指标头（`获必修学分`、`超越员工数`、`完成任务数`）的过滤；
  - 优化 `isCourseCard` 与回退 `markers` 扫描范围，彻底杜绝头部概览数据被误抓为课程小节。
- **单课标题提取与行内交互展示优化**：
  - 小节标题优先直接从单课自身 DOM 元素提取（`titleFrom(item)`），保持小节标题完整原貌；
  - 任务列表中对于线下课、问卷等需本人处理的任务，清晰展示对应的徽章与操作提示。

---

## [1.3.8] - 2026-09-10

### 🚀 核心架构升级与功能增强 (Architecture & Enhancements)

- **单平台统一 Webview 窗口架构（彻底根除多端并发冲突）**：
  - 将原先分离的“选择专题”浏览器窗口与“课程学习”播放器窗口统一收敛为单一平台专属窗口（`video-task-{provider}`）；
  - 选专题导入、刷视频、看课件 PPT、考试答题全场景共用该窗口流转导航，物理消除同账号在多个内置 Webview 间相互踢下线或触发防作弊锁定的隐患。
- **智能防作弊弹窗拦截与无缝自愈**：
  - 注入脚本实时感知平台弹出的“防作弊提醒”、“多端学习检测”等阻断性提示弹窗；
  - 毫秒级自动穿透并模拟点击“继续学习”或确认按钮，自愈学习流，无需人工干预。
- **Webview 开发者工具 (DevTools) 快捷键支持**：
  - 原生开启 Webview DevTools 调试能力；
  - 支持通过 `F12`、`⌥⌘I` (macOS) 或 `Ctrl+Shift+I` (Windows/Linux) 随时调出网页开发者工具，便于分析页面 DOM、排查控制台与网络请求。
- **手动学习防打扰联动**：
  - 在用户手动点击“打开内容/打开课件”（`open_video_course`）时自动暂停后台队列调度，避免用户在手动查看课件或复习视频时被后台自动切课打扰。

### 🛠️ 修复与数据防护 (Fixes & Data Integrity)

- **学习进度单调递增保护与防倒退覆盖**：
  - 彻底修复已学完（100% / `completed`）的课程在手动打开或重新抓取时因网页上报延迟的旧进度（如 79%）导致本地被降级的严重缺陷；
  - 写入数据库（UPSERT）与运行时更新双重防护：已完成课程施加绝对防篡改锁定，未完成课程严格取 `MAX(本地进度, 网页上报进度)`，保证进度单调只增不减。
- **课件类型精准判定与数据净化**：
  - 全面支持 PPT/PPTX 等课件类型徽章识别；
  - 移除启动阶段将 PPT 误变更为视频的残留历史迁移，确保课件独立追踪和长文档阅览机制正常运转。
- **修复后台自动切课导致可见 Webview 窗口被强行关闭**：
  - 修复前一课程完成自动切入下一课程时，调度器底层无条件执行 `window.hide()` 导致用户正打开查看的窗口被强制隐藏关闭的缺陷；
  - 增加窗口可见性动态感知：若用户已主动打开 Webview 窗口查看，切课时保持窗口持续可见并平滑流转下一课，绝不闪退关闭；仅当处于纯后台静默播放时才继续保持隐藏。
- **行级单课精准播放与队列调度防死锁修复**：
  - 解决列表中出现多个“继续队列”且点击无效的严重交互缺陷；
  - 新增 `play_video_course` 原生指令，将单课行内按钮优化为【继续播放】/【继续学习】，点击哪一课便立即激活调度哪一课，真正做到“指哪打哪”；
  - 修复 `open_video_course` 手动查看课件时将课件状态误篡改为 `paused` 且注入僵尸状态导致的队列调度永久死锁问题；
  - 调度看门狗将 PPT 课件（`slides`）纳入长宽容期（300秒），杜绝课件被过早判定为进度停滞而误杀。

---

## [1.3.7] - 2026-09-10

### 🛠️ 修复与稳定性增强 (Fixes & Stability Improvements)

- **彻底消除课程内容文本导致的登录失效误杀（破案级修复）**：
  - 优化底层 JS Bridge 登录失效检测机制，增加**媒体元素与播放器容器存在性豁免**：页面只要检测到 `<video>`、`<audio>` 或播放器容器，绝不上报 `need_login`，彻底避免《接口测试》等涉及系统登录/认证测试的课程正文文本触发误判；
  - 增加 8 秒首屏加载缓冲宽容期，排除 SPA 页面首屏尚未渲染完成时的过渡态误判；
  - 严格收窄登录表单特征判定，必须真实包含密码输入框（`input[type='password']`）或二维码登录区域且无播放器时才触发上报。
- **消除恢复时的暴力刷新打断与二次熔断**：
  - 队列调度器启动课程时增加平滑接管保护：若播放窗口已在当前课程上且处于播放态，平滑接管并应用配置，**严禁无脑重新 `window.navigate` 刷新切断正在进行的播放**。
- **手动播放与队列控制台状态联动解除锁定**：
  - 在手动点击“打开内容”（`open_video_course`）以及点击“登录后继续”（`resume_video_platform`）时，**自动清空 `video_queue_lanes.blocked_reason` 并清除历史登录错误文本**，确保前台播放与后台调度状态实时一致。
- **【开始队列】智能化自动解封与防死锁**：
  - 用户主动点击“开始队列”时，后台自动清理历史因登录失效产生的平台通道阻断标记；
  - 前端放宽按钮可用条件，存在待播或暂停课程时不再死锁变灰，允许用户一键恢复并直接开始运行队列。

---

## [1.3.6] - 2026-09-08

### 🚀 新功能与核心增强 (Features & Enhancements)

- **YS 学堂【文档】与手册类课程全流程自动化调度**：
  - 自动挂载长文档/手册/课件学习进度感知监听（包括右侧实时“学习进度: XX%”、进度条以及页面平滑滚动保活）；
  - 严格等待学习进度达到 100% 后再自动触发完播上报，杜绝固定 75 秒切走导致任务未完成的问题；
  - 调度核心将 `material`（文档/课件）纳入自动流转队列，支持自动播放、重试与重置，并设置 300 秒长宽容期防假卡死看门狗；
  - 数据库启动时自动迁移，无损激活历史导入的未完成文档课程。
- **课件与视频类型智能精准判别**：
  - 优先以课时条目自身的【视频】/【文档】/【课件】/【PPT】徽章和标题后缀为准，杜绝被播放器全局 `<video>` 元素一刀切误判。

### 🛠️ 修复与优化 (Fixes & Improvements)

- **YS 学堂章节手风琴防反向误折叠**：
  - 修复章节容器匹配命中标题自身导致展开状态误判的缺陷；
  - 增加小节元素先验探测：对已展开并包含课时的章节严格禁止点击，彻底杜绝导入时已展开章节被反向关闭的问题。
- **标题元数据过滤精准化（消除“总分”关键词误杀）**：
  - 收窄 `isMeta` 元数据识别正则，严格要求携带冒号或数字，解决《总分联动开发框架》等包含“总分”的正常课程标题被静默过滤的问题。
- **单章节目录与全角括号兼容**：
  - 目录解析支持全角中文括号 `章节（1）`；
  - 支持 `.course-detail-right-wrapper` 容器及单章节课程目录解析。

---

## [1.3.5] - 2026-09-08

### 🏗️ 架构重构 (Architecture Refactoring)

- **银联乐学与 YS 学堂逻辑彻底解耦**：
  - 将抓取解析脚本（`capture_script`）和点击跳转脚本（`course_click_script`）进行彻底物理隔离；
  - 银联乐学采用专属超轻量解析器（`ulearn_capture_script`），大幅瘦身回传数据包，从源头上杜绝 WebKit 频繁修改窗口标题导致的通信丢包或超时中断；
  - YS 学堂专属保留多阶段大纲自动展开、侧边栏定位、大章节纯标题过滤、重复考试去重及复杂弹窗穿透逻辑。

### 🛠️ 修复与增强 (Fixes & Improvements)

- **银联乐学状态精准识别**：
  - 原生支持卡片右上角“已学习”（已完成/100%）与“未学习”（待播放/0%）状态徽章识别；
  - 支持“学时”时长换算（1 学时默认折算为 45 分钟），解决部分课程显示“时长未知”的问题；
  - 支持识别专题总体完成指标（“完成标准 X/Y 门数”、“章节进度 / 学习进度 100%”），当整专题达成时自动校正全量课程完成状态；
  - 导入与同步时自动刷新 `video_topics` 表的完成数与总进度，确保管理台显示完全一致。
- **YS 学堂大纲与切课体验优化**：
  - 支持自动探测并点击展开处于折叠状态的章节手风琴，精准抽取异步小节课程；
  - 优化切课逻辑：同专题内连续播放时复用当前页面，避免重复全页刷新加载。

---

## [1.3.4] - 2026-09-06

### 🛠️ 修复 (Fixes)

- **废除 90%~98.5% 停滞“假完播”**：彻底移除进度超 90% 停滞强行标记完播的逻辑，避免因中途答题卡、防挂机弹窗或网络卡顿导致平台学时未结算而本地谎报 100%；中途停滞统一如实标记为异常并保留真实进度。
- **前置登录失效优先阻断**：窗口 URL 重定向至登录页时优先阻断平台通道并提示登录，彻底解决 60 秒停滞抢跑导致后续课程被连续误报跳过的问题。
- **数据库写操作严格错误校验**：消除所有可能静默忽略 SQL 执行结果的隐患，保证课程与专题状态一致。

### 🚀 优化与改进 (Improvements)

- **分层超时机制**：
  - 页面打开与加载（`opening`）阶段超时放宽至 120 秒，保护富媒体慢加载与弱网环境；
  - 播放中停滞（`playing`）超时调整为 120 秒，兼顾卡顿缓冲与死锁自愈。
- **高精度饱和完播**：保留 `>= 98.5%` 或剩余不到 1.5 秒且停滞 4 秒的末尾饱和完播看门狗，解决部分教学平台在最后一秒仅执行 `pause` 而不派发标准 `ended` 事件的问题。

---

## [1.3.3] - 2026-09-06

### 🛠️ 修复 (Fixes)

- **学习任务“完成核验中（100%）”卡死死锁**：修复网页定时上报 `ended` 消息时无条件刷新 `active.phase_since` 时间戳的问题，彻底解决调度器缓冲判断 `now() - active.phase_since < 2` 永远无法达成导致的死循环停滞。

### 🚀 看门狗（Watchdog）防卡死系统 (Watchdog System)

- **状态自锁与核验缓冲流转**：进入 `ended` 阶段时锁定时间戳，禁止网页重复上报刷新时间戳，预留 2 秒缓冲后正规完成流转并调度下一节。
- **前端调度防死锁探活**：在学习任务管理台前端轮询中增加 10 秒超时强制解锁机制，防止任何单次 IPC 挂起导致定时调度器永久休克。

---

## [1.3.2] - 2026-09-06

### 🚀 优化与改进 (Improvements)

- **按专题顺序播放**：按专题列表及专题内课程顺序调度，播完当前专题再进入下一个；同步时间、重试时间不再改变播放顺序。跨站并行时，两个平台各自顺序播放一门课程。
- **暂停与跳过分离**：暂停队列保留当前进度，再次开始时优先续播；跳过的课程保留为“已跳过”，需要手动重试才重新加入队列。断点续播以平台保存的进度为准。
- **播放顺序提示**：管理台展示当前课程和“下一节”，并新增已暂停、已跳过统计。

### 🛠️ 修复 (Fixes)

- 登录失效时暂停对应平台，保留当前课程，完成登录后可继续；其他平台可正常运行，避免后续课程连续失败。
- 同步专题和重启应用后保留暂停、跳过及异常状态；平台确认已完成时仍正常更新完成状态。
- 取消已暂停或已跳过课程的延迟播放操作，避免旧任务重新触发播放；预览下一节时不再修改队列位置。
- 从跨站并行切换为串行时先暂停现有播放，再按单路队列恢复；暂停、跳过、同步与队列轮询共用调度锁，减少切换冲突。

---

## [1.3.1] - 2026-09-05

### 🛠️ 修复 (Fixes)

- **课程完成状态同步**：修复 YS学堂已完成的课程因显示“上次学习”或处于选中状态，同步后被误判为“待播放、0%”的问题；保留真正未完成课程的状态和进度。
- **队列首次启动**：修复首次点击“开始队列”只创建播放窗口、随后自动暂停，需要再次点击才能启动课程的问题。

### 🚀 优化与改进 (Improvements)

- 启动按钮增加“正在启动”提示并阻止重复点击；按钮与定时轮询共用调度锁，避免重复启动同一课程。
- 课程启动失败时显示具体错误，避免失败后仍提示“队列已开始运行”。
- 新增 41 项课程完成状态回归检查，覆盖当前选中章节、“上次学习”、完成对勾及未完成状态等场景。

---

## [1.3.0] - 2026-09-04

### 🚀 优化与改进 (Improvements)

- **课程类型智能判定重构**：
  - 优先检测页面真实 DOM 特征（`<video>` 播放元素、播放器容器及视频时长）；
  - 彻底解决由于课程标题包含“PPT”、“课件”等字样被误判为课件的问题，时长 >=3 分钟的视频课程优先按视频正常播放；
  - 前端展示与队列调度完全遵从后端判定的真实类型，修复课件和视频类型混淆。

- **章节目录与完成状态高精度提取**：
  - 自动识别并过滤“模块X”、“第X阶段”、“第X期”等层级父标题，彻底避免吞并多期次课程；
  - 消除相邻章节对勾渗透问题，严格将状态检测限定在当前章节节点内部，避免前一章节对勾污染后续章节；
  - 排除时钟图标等通用 SVG 折线标签（`<polyline>`）的误判，避免“上次学习”等未学完章节误标为已完成；
  - 精准区分“未开始（橙圈）”、“学习中/未学完（绿圈/上次学习）”与“已完成（对勾）”。

- **界面交互与体验微调**：
  - 专题与单课“重置”按钮图标更换为撤销回退图标 `Undo2`（↩），彻底消除与“同步”旋转箭头（`RefreshCw`）的视觉混淆；
  - 移除顶栏冗余的“刷新”按钮，操作界面更为精炼清爽；
  - 移除调试临时文件写入，彻底杜绝本地开发环境点击同步/导入触发前端 Vite HMR 导致整页强制刷新的问题。

- **播放窗口与课件稳定性**：
  - 修复打开课件/课程时窗口闪退与一闪而逝的问题；
  - 优化任务互斥机制与会话状态同步。

---

## [1.2.9] - 2026-09-02

### 🚀 新增特性 (Features)

- **单课播放页/章节目录智能导入**：
  - 支持量见·云课堂等平台在单门视频课播放页中直接一键批量导入右侧所有章节；
  - 智能解析章节名称、时长与 DOM 定位器，彻底排除播放器控件与倍速菜单等干扰项；
  - 课程大标题自动精准识别（如《天龙八步™-极简项目管理》）。
- **选专题/登录窗口悬浮导航工具条**：
  - 在【登录并选择专题】窗口中注入磨砂质感导航胶囊工具栏；
  - 提供 **后退**（`Alt + ←`）、**前进**（`Alt + →`）、**刷新**、**返回首页** 及 **自由拖拽** 功能，极大提升浏览选课体验。
- **安全合规与域名混淆脱敏**：
  - 平台域名采用 Base64 编码与运行时动态解码，代码库中彻底去除敏感域名明文；
  - 平台展示名称统一更新为 **YS学堂**，全方位清理相关敏感中文关键字。

### 🛠️ 体验与稳定性优化 (Improvements & Fixes)

- **完播即刻流转**：
  - 移除原验证等待超时机制；视频播放完毕或检测到“已完成”提示卡片时，即刻更新为 100% 完成并毫秒级切入下一门待播课程。
- **互动小测验与防挂机智能脱困**：
  - 新增停滞超时检测，视频中间弹出小单选测试题时自动选择并提交，全覆盖识别“继续学习/确定/知道了”等防挂机弹窗。
- **多窗口架构与强互斥清洗**：
  - 选专题窗口与后台播放窗口彻底解耦，互不干扰且共享登录 Session；
  - 同一平台强互斥保证唯一活跃播放任务，杜绝多任务同时处于“正在打开”状态。
- **消除初始白屏闪烁**：
  - 播放窗口后台隐藏构建，彻底消除启动时的白色窗口闪烁。

---

## [1.2.8] - 2026-08-30

- 视频学习任务多平台支持（银联乐学 / YS学堂）。
- 静默后台播放与自定义倍速、静音设置。
- 基础功能与跨平台打包支持。
