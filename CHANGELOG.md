# Changelog

## [1.0.1]

- **文本对比 (FileDiff)**：
  - 新增输入文件大小限制 (10MB)，防止浏览器原生拖拽等方式导致的内存溢出 (OOM)。
  - 新增超大文件 (>10000行) 的降级算法，由底层全量比对切换为逐行极速比对，并在界面顶部增加明显的大文件降级警告提示。
  - 修复多处硬编码的英文报错，并实现报错信息的完整国际化（支持中英文）。
  - 优化高亮差异字词的内部渲染性能。
- **JSON 格式化 (JsonFormatter)**：
  - 在「原始输入」面板左侧新增代码行号显示列，方便定位报错中的语法错误。
  - 在「格式化输出」面板中新增了**嵌套节点折叠与展开功能**，并精确对齐视觉缩进。
  - 修复了 `escapeHtml` 无法正确全局转义长字符串带 `<`、`>` 符号的问题。
- **其他维护**：
  - 修复 `App.tsx` 中关于自动更新信息 `updater.updateInfo?.version` 缺少依赖的 React 警告。

## [1.0.20]

- 测试跳过版本功能（跳过此版本后不再自动弹出）

## [1.0.19]

- 更新弹窗新增「跳过此版本」按钮，跳过后重启不再自动弹出

## [1.0.18]

- 测试启动时自动弹出更新弹窗

## [1.0.17]

- 启动时自动检测新版本并弹出更新弹窗

## [1.0.16]

- 修复自动更新后重启权限问题
- 更新检查失败时显示具体错误信息
- 修复版本号显示为硬编码问题

## [1.0.15]

- 修复自动更新权限配置，updater/process 权限现已正确启用
- 自动更新流程首次完整验证通过

## [1.0.14]

- 修复自动更新检测失效问题（Tauri capabilities 缺少 updater:default 权限）
- 修复更新安装后重启失败问题（缺少 process:default 权限）

## [1.0.13]

- 检查更新失败时在界面显示具体错误信息（红色文字）

## [1.0.12]

- 当前版本号改为动态读取，不再显示硬编码的 v1.0.0

## [1.0.11]

- 修复 CI 签名流程，gen-latest-json 改为 .cjs 格式
- 修复 latest.json 平台键（darwin-aarch64 / darwin-x86_64）
- CI 流水线首次全绿
